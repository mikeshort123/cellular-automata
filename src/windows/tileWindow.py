import pygame

class TileWindow:

    def __init__(self):
        return

    def tick(self, handler):
        return

    def render(self, renderer):
        pygame.draw.rect(renderer.display, (255,255,0), (100,100,100,100))
